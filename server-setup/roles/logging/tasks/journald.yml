---
- name: Copy journald configuration
  ansible.builtin.copy:
    src: journald.conf
    dest: /etc/systemd/journald.conf
    owner: root
    group: root
    mode: "0644"
    backup: "yes"
  become: true
  notify: Restart journald
