all:
  hosts:
    remotehost:
      ansible_connection: ssh
      ansible_host: <host> # set host (either IP or alias)
      ansible_user: <user> # set user for whom qBittorrent should be installed
      ansible_port: 22
      ansible_ssh_private_key_file: ~/.ssh/{{ ssh_private_key_filename }}
